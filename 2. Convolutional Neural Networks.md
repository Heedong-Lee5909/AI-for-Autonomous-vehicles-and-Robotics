# Convolutional Neural Networks (CNN)

---

## 개요
CNN은 이미지와 같이 **격자형 구조의 데이터**를 처리하도록 설계된 딥러닝 모델이다.
시각적 인식 작업에 특히 효과적이며, 현대 컴퓨터 비전의 핵심 기술이다.

---

## 핵심 개념

### 1) 합성곱 연산 (Convolution Operation)
- **정의:** 두 함수를 결합하여 한 함수의 형태가 다른 함수에 의해 변형되는 방식을 나타내는 수학적 연산
- **목적:** 필터(커널)를 적용하여 입력 데이터에서 특징 추출

**수식:**
```
(I * K)(i, j) = Σm Σn I(i−m, j−n) · K(m, n)
```

| 구성 요소 | 설명 |
|-----------|------|
| (I∗K)(i,j) | 위치 (i,j)에서의 합성곱 결과 |
| I(i−m, j−n) | 입력 이미지에서 위치 (i−m, j−n)의 픽셀 값 |
| K(m,n) | 위치 (m,n)에서의 커널 값 |
| Σm Σn | 커널 차원에 대한 합산 |

### 2) 필터 / 커널 (Filters/Kernels)
- 특정 특징을 감지하는 소형 행렬 (예: 3×3, 5×5)
- 필터의 값은 **학습 과정에서 자동으로 학습**됨

### 3) 특징 맵 (Feature Maps)
- 합성곱 연산의 출력 결과
- 다양한 위치에서 특징(엣지, 텍스처 등)의 존재를 나타냄

### 4) 활성화 함수 (Activation Functions)
네트워크에 **비선형성**을 도입하는 함수

| 함수 | 수식 | 특징 |
|------|------|------|
| **ReLU** | max(0, x) | 가장 널리 사용, 음수는 0으로 처리 |
| **Sigmoid** | 1 / (1 + e⁻ˣ) | 출력값을 0~1 사이로 변환 |
| **Tanh** | (eˣ − e⁻ˣ) / (eˣ + e⁻ˣ) | 출력값을 -1~1 사이로 변환 |

### 5) 풀링 레이어 (Pooling Layers)
특징 맵의 **공간적 차원을 축소**하여 연산량 감소

| 종류 | 방법 |
|------|------|
| **Max Pooling** | 각 윈도우에서 최댓값 선택 |
| **Average Pooling** | 각 윈도우의 평균값 계산 |

**Max Pooling 수식:**
```
Y(i, j) = max(0≤m<p) max(0≤n<p) X(p·i+m, p·j+n)
```

| 구성 요소 | 설명 |
|-----------|------|
| Y(i,j) | 위치 (i,j)에서 풀링 후 출력값 |
| X | 입력 특징 맵 |
| p | 풀링 윈도우 크기 |
| max | 최대 함수 |

### 6) 완전 연결 레이어 (Fully Connected Layers)
- 이전 레이어의 모든 뉴런과 완전히 연결
- 최종 **분류(Classification)** 역할 수행

---

## CNN 아키텍처 구성 요소

```
입력 레이어 → 합성곱 레이어 → 활성화 레이어(ReLU) → 풀링 레이어
→ [반복] → 평탄화 레이어 → 완전 연결 레이어 → 출력
```

| 레이어 | 역할 |
|--------|------|
| 입력 레이어 | 원시 픽셀 데이터 수신 (높이 × 너비 × 3) |
| 합성곱 레이어 | 필터로 특징 추출 |
| 활성화 레이어 | 비선형 함수 적용 (ReLU 등) |
| 풀링 레이어 | 특징 맵 다운샘플링 |
| 평탄화 레이어 | 2D → 1D 벡터 변환 |
| 완전 연결 레이어 | 특징 기반 분류 수행 |

---

## CNN의 작동 원리

### 특징 추출
- **초기 레이어:** 저수준 특징 포착 (엣지, 모서리)
- **깊은 레이어:** 고수준 특징 포착 (객체, 형태)

### 분류
완전 연결 레이어가 추출된 특징을 분석하여 최종 분류 수행

---

## CNN 학습 과정

### 1) 순전파 (Forward Propagation)
입력 데이터가 네트워크를 통해 전달되어 출력 생성

### 2) 손실 함수 (Loss Function)
예측값과 실제값의 차이 측정 — **교차 엔트로피 손실 (Cross-Entropy Loss)**
```
L = −Σi yi · log(pi)
```

| 구성 요소 | 설명 |
|-----------|------|
| L | 손실 값 |
| yᵢ | 실제 레이블 (정답이면 1, 아니면 0) |
| pᵢ | 클래스 i에 대한 예측 확률 |

### 3) 역전파 (Back Propagation)
- 손실 함수의 가중치에 대한 기울기 계산
- 경사 하강법으로 가중치 업데이트

### 4) 최적화 알고리즘

**SGD (확률적 경사 하강법):**
```
w ← w − α∇L(w)
```

| 구성 요소 | 설명 |
|-----------|------|
| w | 네트워크 가중치 |
| α | 학습률 (Learning Rate) |
| ∇L(w) | w에 대한 손실의 기울기 |

**적응형 방법:**
- **Adam** (적응적 모멘트 추정): 학습률 자동 조정
- **RMSprop** (평균 제곱근 전파): 학습률 자동 조정

---

## 주요 CNN 아키텍처

| 아키텍처 | 특징 |
|----------|------|
| **LeNet-5** | 숫자 인식용, 최초의 실용적 CNN |
| **AlexNet** | 2012 ImageNet 우승, ReLU·드롭아웃 도입 |
| **VGGNet** | 3×3 소형 필터 사용, VGG16·VGG19 변형 |
| **GoogLeNet (Inception)** | Inception 모듈로 다중 스케일 특징 포착 |
| **ResNet** | 잔차 학습으로 매우 깊은 네트워크 학습 가능 |

**ResNet 잔차 블록 수식:**
```
y = F(x, {Wᵢ}) + x
```

| 구성 요소 | 설명 |
|-----------|------|
| y | 잔차 블록의 출력 |
| F(x, {Wᵢ}) | 합성곱 레이어를 나타내는 함수 |
| x | 블록에 대한 입력 |
| {Wᵢ} | 합성곱 레이어의 가중치 |

> 입력 x를 출력에 직접 더해 **기울기 소실 문제** 해결

---

## CNN의 응용 분야

- 🖼️ **이미지 분류** — 이미지를 카테고리로 분류
- 🔍 **객체 탐지** — 이미지 내 객체의 위치 및 종류 탐지
- 🗺️ **의미론적 분할** — 픽셀 단위 장면 이해
- 👤 **얼굴 인식** — 얼굴 식별 및 인증
- 🏥 **의료 이미지 분석** — X-ray, MRI 등 진단 지원
- 🎬 **영상 분석** — 동영상 내 동작 및 이벤트 인식

---

*AI for Autonomous Vehicles and Robotics 강좌 정리*
